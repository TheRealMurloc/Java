package nai.zad4;

public class Point 
{
	/*
	double y;
	double x;
	
	public Point(int x, int y)
	{
		this.y = y;
		this.x = y;
	}
	
	
	public double calculateDistance(Centroid a)
	{
		double distance;
		distance = Math.pow(((this.x - a.x) + (this.y - a.y)), 2);
		distance = Math.pow(distance, 0.5);
		return distance;
	}
	
	*/

}
